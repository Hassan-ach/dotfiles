partial modifier_keys
xkb_symbols "my_modkeys" {
    // Caps Lock → Control
    replace key <CAPS> { [ Control_L, Control_L ] };
    modifier_map Control { <CAPS> };
    
    // Left Alt → Left Shift  
    replace key <LALT> { [ Shift_L, Shift_L ] };
    modifier_map Shift { <LALT> };
};
