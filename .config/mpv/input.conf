# =========================
# MPV INPUT CONFIG with Cheatsheet
# Save as: ~/.config/mpv/input.conf
# =========================

# === PLAYBACK CONTROL ===
SPACE cycle pause                  # ⏯ Play / Pause
p cycle pause                      # ⏯ Play / Pause
. frame-step                       # ▶️  Next frame (pause must be on)
, frame-back-step                  # ◀️  Previous frame (pause must be on)

> multiply speed 1.25; show-text "Speed: ${speed}"  # ⏩ Speed up ×1.25
< multiply speed 0.8; show-text "Speed: ${speed}"   # 🐢 Slow down ×0.8
= set speed 1.0; show-text "Speed: ${speed}"        # ⏱ Reset speed to 1.0
+ add speed 0.1; show-text "Speed: ${speed}"        # ⏱ Speed +0.1
- add speed -0.1; show-text "Speed: ${speed}"       # ⏱ Speed -0.1

# === VOLUME ===
h add volume -1; show-text "Vol: ${volume}%"   # 🔉 Volume -1
l add volume 1; show-text "Vol: ${volume}%"    # 🔊 Volume +1
H add volume -5; show-text "Vol: ${volume}%"   # 🔉 Volume -5
L add volume 5; show-text "Vol: ${volume}%"    # 🔊 Volume +5
m cycle mute; show-text "Muted: ${mute}"       # 🔇 Toggle mute

# === SEEKING ===
j seek -10; show-progress   # ◀️  Seek -10s
k seek 10; show-progress    # ▶️  Seek +10s
J seek -30; show-progress   # ◀️  Seek -30s
K seek 30; show-progress    # ▶️  Seek +30s
Ctrl+d seek 60; show-progress # ⏩ Seek +60s
Ctrl+u seek -60; show-progress # ⏪ Seek -60s
g seek 0 absolute-percent; show-progress    # ⏮ Go to start
G seek 100 absolute-percent; show-progress  # ⏭ Go to end

# Seek to percentage
1 seek 10 absolute-percent; show-progress
2 seek 20 absolute-percent; show-progress
3 seek 30 absolute-percent; show-progress
4 seek 40 absolute-percent; show-progress
5 seek 50 absolute-percent; show-progress
6 seek 60 absolute-percent; show-progress
7 seek 70 absolute-percent; show-progress
8 seek 80 absolute-percent; show-progress
9 seek 90 absolute-percent; show-progress
0 seek 0 absolute-percent; show-progress

# === VIDEO CONTROL ===
f cycle fullscreen     # 🖥 Toggle fullscreen
Alt+0 set window-scale 0.5 # 📏 Window scale 0.5×
Alt+1 set window-scale 1.0 # 📏 Window scale 1×
Alt+2 set window-scale 2.0 # 📏 Window scale 2×

# === SUBTITLE CONTROL ===
s cycle sub                     # 📝 Cycle subtitles
S cycle sub-visibility          # 📝 Toggle subtitles
a cycle audio                   # 🎧 Cycle audio tracks
c cycle sub-forced-only         # 📝 Toggle forced-only subs
Ctrl+Shift+h add sub-delay -0.1; show-text "Sub delay: ${sub-delay}" # ⏳ Sub delay -0.1s
Ctrl+Shift+l add sub-delay 0.1; show-text "Sub delay: ${sub-delay}"  # ⏳ Sub delay +0.1s
Ctrl+j add sub-scale -0.1; show-text "Sub scale: ${sub-scale}"       # 🔠 Sub size smaller
Ctrl+k add sub-scale 0.1; show-text "Sub scale: ${sub-scale}"        # 🔠 Sub size bigger

# === QUALITY CONTROL (YouTube) ===
# Interactive quality menu (requires youtube-quality.lua script)
# Ctrl+f script-message toggle-quality-menu
# Quick quality cycles (backup)
Ctrl+q cycle-values ytdl-format "best[height<=1080]" "best[height<=720]" "best[height<=480]" "best[height<=360]" "worst"


# === PLAYLIST NAVIGATION ===
n playlist-next      # ⏭ Next playlist item
N playlist-prev      # ⏮ Previous playlist item

# === CHAPTERS ===
Ctrl+n add chapter 1   # 📖 Next chapter
Ctrl+p add chapter -1  # 📖 Previous chapter

# === INFO & OSD ===
i script-binding stats/display-stats-toggle  # 📊 Toggle stats
I script-binding stats/display-page-4        # 📊 Stats page 4
o show-progress                              # 📊 Show progress bar
O no-osd cycle-values osd-level 3 1          # 📊 Toggle OSD detail

# === BOOKMARKS ===
b set time-pos 0       # 🔖 Jump to start
B quit-watch-later     # 🔖 Save position & quit

# === SCREENSHOTS ===
t screenshot           # 📷 Screenshot
T screenshot video     # 📷 Screenshot (video only)

# === LOOPING ===
Ctrl+Shift+l cycle-values loop-file "inf" "no"     # 🔁 Loop file
Ctrl+Shift+L cycle-values loop-playlist "inf" "no" # 🔁 Loop playlist

# === EXIT / WINDOW CONTROL ===
ESC set fullscreen no  # 🚪 Exit fullscreen
q quit                 # 🚪 Quit
Q quit-watch-later     # 🚪 Quit & save position
Ctrl+c quit 4          # 🚪 Force quit
