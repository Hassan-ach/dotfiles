# Default config for sway
#
# Copy this to ~/.config/sway/config and edit it to your liking.
#
# Read `man 5 sway` for a complete reference.

### Variables
#
# Logo key. Using Mod4 (Super/Windows key) as per your i3 config.
set $mod Mod4

# Home row direction keys, like vim. Kept from default Sway.
set $left h
set $down j
set $up k
set $right l

# Your preferred terminal emulator. Changed from 'foot' to 'kitty' as per your i3 config.
set $term kitty

# Your preferred application launcher. Changed from 'wmenu-run' to 'rofi' as per your i3 config.
set $menu rofi -show drun

# Monitor variables from your i3 config.
# Note: In Sway, monitor names often look like 'eDP-1', 'HDMI-A-1'.
# You can verify yours by running: swaymsg -t get_outputs
set $fm eDP-1
set $sm HDMI-A-1

### Output configuration
#
# Default wallpaper. Changed from default Sway wallpaper to your specified image.
# If the path is relative, it will be relative to your home directory.
# `fill` scales the image to fit the output while preserving aspect ratio.
output * bg ~/bgs/cool-bg.jpg fill

# Example configuration for your second monitor from i3.
# This replaces the xrandr command from your i3 config.
# Adjust the position (e.g., 1920,0) and resolution (1600x900) as needed for your setup.
# Run `swaymsg -t get_outputs` to find exact names and current settings.
output $sm resolution 1600x900 position 1920,0

### Idle configuration
#
# This section is from the default Sway config, providing automatic screen locking and power off.
# It uses 'swaylock' for locking, which is the native Wayland locker for Sway.
# Your i3 config had 'xss-lock' and 'i3lock', which are X11 specific.
#
exec swayidle -w \
         timeout 300 'swaylock -f -c 000000' \
         timeout 600 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' \
         before-sleep 'swaylock -f -c 000000'

### Input configuration
#
# Configures input devices. Replaces xinput commands from your i3 config.
#
input type:touchpad {
    dwt enabled
    tap enabled
    natural_scroll enabled
    middle_emulation enabled
}

input type:keyboard {
    # Sets keyboard layout. Uncomment and adjust if needed.
    # xkb_layout "us"

    # Sets keyboard options, like "ctrl:nocaps" from your i3 config.
    xkb_options "ctrl:nocaps"
    xkb_options "alt_l:shift"
    # xkb_options "lv3:ralt_switch" # If you need this option
}

# The `~/.local/bin/keyboard-setup.sh` script is commented out.
# If it contains only `xkb_layout` or `xkb_options` settings,
# translate them to the `input type:keyboard` block above.
# If it contains other X11-specific commands, they will likely not work in Wayland.
# exec_always --no-startup-id ~/.local/bin/keyboard-setup.sh

### Window Borders and Opacity
# Removes borders and titlebars for all windows
default_border none


### Key bindings
#
# Basics:
# Start a terminal. Uses 'kitty' as specified in your variables.
bindsym $mod+Return exec $term

# Kill focused window
bindsym $mod+Shift+q kill

# Start your primary launcher (rofi drun mode)
bindsym $mod+d exec $menu

# Start rofi window switcher (mod+Tab from your i3 config)
bindsym $mod+Tab exec rofi -show window

# Start rofi powermenu (mod+p from your i3 config)
bindsym $mod+p exec ~/.config/rofi/powermenu.sh

# Drag floating windows by holding down $mod and left mouse button.
# Resize them with right mouse button + $mod.
# Despite the name, also works for non-floating windows.
# Change normal to inverse to use left mouse button for resizing and right
# mouse button for dragging.
floating_modifier $mod normal

# Reload the configuration file
bindsym $mod+Shift+c reload

# Exit sway (logs you out of your Wayland session)
# Uses swaynag for confirmation, similar to i3-nagbar.
bindsym $mod+Shift+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -B 'Yes, exit sway' 'swaymsg exit'

# Unlike i3, Sway handles its own restarts seamlessly without a separate binding.
# The 'restart' command will just reload the configuration.
# bindsym $mod+Shift+r restart # Not explicitly needed as reload covers this for Sway

# Lockscreen binding. Uses swaylock instead of your i3lock script.
bindsym $mod+c exec swaylock -f -c 000000

# Moving around:
# Move your focus around using h/j/k/l
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right
# Or use $mod+[up|down|left|right] arrow keys
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move the focused window with the same, but add Shift
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right
# Ditto, with arrow keys
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Workspaces:
# Define names for default workspaces and assign them to monitors, similar to your i3 config.
# Make sure $fm and $sm are correctly set to your monitor names.
workspace number 1 output $fm
workspace number 2 output $fm
workspace number 3 output $fm
workspace number 4 output $fm
workspace number 5 output $fm
workspace number 6 output $fm
workspace number 7 output $fm
workspace number 8 output $sm
workspace number 9 output $sm
workspace number 10 output $sm

# Switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

# Move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10
# Note: workspaces can have any name you want, not just numbers.
# We just use 1-10 as the default.

# Layout stuff:
# You can "split" the current object of your focus with
# $mod+b or $mod+v, for horizontal and vertical splits respectively.
bindsym $mod+b splith
bindsym $mod+v splitv

# Switch the current container between different layout styles
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# Make the current focus fullscreen
bindsym $mod+f fullscreen

# Toggle the current focus between tiling and floating mode
bindsym $mod+Shift+space floating toggle

# Swap focus between the tiling area and the floating area
bindsym $mod+space focus mode_toggle

# Move focus to the parent container
bindsym $mod+a focus parent

# Scratchpad:
# Sway has a "scratchpad", which is a bag of holding for windows.
# You can send windows there and get them back later.

# Move the currently focused window to the scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the next scratchpad window or hide the focused scratchpad window.
# If there are multiple scratchpad windows, this command cycles through them.
# The `fullscreen` command from your i3 config is omitted as it's not default Sway behavior for scratchpad.
bindsym $mod+minus scratchpad show

# Resizing containers:
# Uses the same keybindings for resize mode as your i3 config.
mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px

    # Ditto, with arrow keys
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"

# Utilities:
# Special keys to adjust volume via PulseAudio. Using pactl commands, similar to your i3 config.
# Note: --locked means the binding works even when the screen is locked.
bindsym --locked XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle
bindsym --locked XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -5%
bindsym --locked XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +5%

# Microphone mute/unmute. Using pactl as in Sway default.
bindsym --locked XF86AudioMicMute exec pactl set-source-mute @DEFAULT_SOURCE@ toggle

# Special keys to adjust brightness via brightnessctl.
bindsym --locked XF86MonBrightnessDown exec brightnessctl set 5%-
bindsym --locked XF86MonBrightnessUp exec brightnessctl set 5%+

# Screenshot: Uses grim (native Wayland screenshot tool) and slurp (for selecting regions).
# Replaces 'scrot' from your i3 config.
bindsym Print exec grim
bindsym Shift+Print exec grim -g "$(slurp)"

# F-key bindings from your i3 config, adapted for Sway/Wayland tools.
bindsym F1 exec pactl set-sink-mute @DEFAULT_SINK@ toggle
bindsym F2 exec pactl set-sink-volume @DEFAULT_SINK@ -5%
bindsym F3 exec pactl set-sink-volume @DEFAULT_SINK@ +5%
bindsym F4 exec brightnessctl set 5%-
bindsym F5 exec brightnessctl set +5%
bindsym F9 exec pactl set-source-mute @DEFAULT_SOURCE@ toggle

### Startup Applications / Services
# These are `exec` commands that run when Sway starts.
# Removed X11 specific commands like picom, polybar, feh (replaced by output bg).
exec_always killall waybar
exec_always waybar &

# tmux session startup
exec tmux source-file ~/.tmux.conf

# NetworkManager Applet (runs via XWayland if needed)
exec nm-applet

# XDG Autostart. Sway often handles this automatically. If you have `.desktop` files
# in ~/.config/autostart, they should launch. If not, uncomment the next line:
# exec --no-startup-id dex --autostart --environment sway

### Window Borders and Opacity
# Sets default border to 0 pixel, similar to `for_window [class="^.*"] border pixel 0` in i3.
default_border pixel 0

# Opacity configuration `for_window [class=".*"] opacity 1` is not directly translatable
# as a global setting in Sway's config. Compositing is handled by Sway itself.
# If you desire transparency for specific windows, you might need rules for them
# or an external compositor like `swayfx` (a fork of Sway).

### Theming (Window Borders):
# Using your Rose Pine theme for window borders, similar to your i3 config's `client.*` settings.
set $base           #191724
set $surface        #1f1d2e
set $overlay        #26233a
set $muted          #6e6a86
set $subtle         #908caa
set $text           #e0def4
set $love           #eb6f92
set $gold           #f6c177
set $rose           #ebbcba
set $pine           #31748f
set $foam           #9ccfd8
set $iris           #c4a7e7
set $highlightlow   #21202e
set $highlightmed   #403d52
set $highlighthigh  #524f67

# Theme for window borders, background, text, and indicators.
#                        border            background          text                indicator          child_border
client.focused           $rose             $base               $text               $rose              $rose
client.focused_inactive  $text             $base               $text               $subtle            $surface
client.unfocused         $text             $base               $text               $overlay           $overlay
client.urgent            $text             $base               $text               $love              $love
client.placeholder       $base             $base               $text               $overlay           $overlay
client.background        $base

### Gaps
# External gaps (between window and screen edge)
gaps outer 00
# Internal gaps (between windows)
gaps inner 00
# gaps top -5 # This is i3 specific and not directly supported in Sway's gaps command.

### Status Bar:
# Read `man 5 sway-bar` for more information about this section.
# bar {
#     position top
#
#     # Set the status_command to use i3status.
#     # i3status will read its configuration from ~/.config/i3status/config by default.
#     status_command i3status
#
#     # Font for the bar. Using your specified i3 font.
#     font pango:Iosevka Term 12
#
#     # Colors for the bar, adapted from your Rose Pine theme in i3.
#     # These map to swaybar's color scheme.
#     colors {
#         background           #191724
#         statusline           #e0def4
#         separator            #6e6a86
#
#         # Workspace colors
#         # border  background text
#         focused_workspace    #ebbcba #191724 #e0def4
#         active_workspace     #1f1d2e #1f1d2e #e0def4
#         inactive_workspace   #26233a #26233a #908caa
#         urgent_workspace     #eb6f92 #eb6f92 #e0def4
#         binding_mode         #c4a7e7 #191724 #e0def4
#     }
# }


include /etc/sway/config.d/*
