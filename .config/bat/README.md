# 🦇 Bat Configuration (`.config/bat`)

Syntax highlighting `cat` replacement configuration.

---

## 📂 Files

- **`config`**: Core options (paging, theme selection, file extensions).
- **`themes/Matugen.tmTheme`**: Dynamic tmTheme color file generated by Matugen (`templates/terminal/bat.tmTheme`).
